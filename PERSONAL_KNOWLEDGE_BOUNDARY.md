# Personal Knowledge Boundary

The distributable SIMS Writer package must not contain real tenant/blog names, tenant URLs, owner-specific article IDs, or owner-only operational history.

Allowed:
- synthetic examples such as `example.invalid`, `sample-site`, and `A900xxx`;
- generalized editorial rules, contracts, schemas, and regression fixtures.

Tenant-specific context belongs in current Evidence or external Personal Editorial Knowledge.
