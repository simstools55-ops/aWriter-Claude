# Claude Repository Operations v1.0

Claude repositories are runtime packages, not separate products. Each product uses a separate Claude Project. Install only product instructions, runtime, contract, validation, knowledge, and scoped Shared snapshot. Updates must distinguish Replace, Add, and Remove. Major identity or snapshot changes require full file replacement and regression testing.
