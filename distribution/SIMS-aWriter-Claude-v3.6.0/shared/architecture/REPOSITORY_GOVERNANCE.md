# Repository Governance v1.0

Each repository must contain README.md, VERSION, PRODUCT_IDENTITY.json, CHANGELOG.md, and SHARED_VERSION where applicable. Product and Claude repositories consume released product-scoped snapshots. Contracts are edited only in Shared. Release requires clean Git state, identity consistency, version consistency, UTF-8 text, passing tests, and a compatibility entry.
